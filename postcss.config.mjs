/**
 * Tailwind is currently broken in this environment (missing tailwindcss/dist/lib.js).
 * To unblock the app, disable Tailwind/PostCSS processing for now.
 */
const config = {
  plugins: {},
};

export default config;

